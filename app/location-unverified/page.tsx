import { locationUnverifiedStartups, remoteWork } from "../data";

export default function LocationUnverifiedPage(){
  return <main><header className="innerNav shell"><a className="brand" href="/"><span className="brandMark">ISM</span><span>India Startup Map</span></a><a href="/explore">← Back to directory</a></header><section className="listingHero shell"><div className="eyebrow">DATA QUALITY / LOCATION UNKNOWN</div><h2>Location needs <em>verification</em></h2><p>{locationUnverifiedStartups.length.toLocaleString()} unique organizations are excluded from the map because only an India association—not a reliable city or office address—is available. Missing location does not imply remote work.</p></section><section className="startupGrid shell">{locationUnverifiedStartups.map(startup=><a href={`/startup/${startup.slug}`} className="startupCard" key={startup.slug}><div className="cardHead"><span className="logo" style={{background:startup.color}}><span>{startup.initials}</span><img src={startup.logoUrl} alt={`${startup.name} logo`}/></span><b className="demoTag">LOCATION UNKNOWN</b></div><h3>{startup.name}</h3><p>{startup.description}</p><div className="meta">REMOTE: {remoteWork[startup.slug]?.label??"UNKNOWN"}<br/>{startup.sector}</div><div className="cardFoot"><span>Not shown on map</span><b>View profile →</b></div></a>)}</section></main>;
}
